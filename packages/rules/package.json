{
  "name": "eslint-plugin-security-rules",
  "version": "0.0.2",
  "description": "",
  "main": "./lib/index.js",
  "files": [
    "lib/**"
  ],
  "keywords": [
    "eslint",
    "eslintplugin"
  ],
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "watch": "yarn build --watch",
    "prepublish": "cp ../../LICENSE ./LICENSE && cp ../../README.md ./README.md && yarn build",
    "postpublish": "rm LICENSE; rm README.md"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:lasselupe33/eslint-plugin-security-rules.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/eslint": "^7.28.0",
    "@types/estree": "^0.0.50",
    "@types/node": "^16.10.1",
    "typescript": "^4.4.3"
  },
  "peerDependencies": {
    "eslint": ">=0.8.0"
  }
}
