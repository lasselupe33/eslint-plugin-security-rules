{
  "name": "eslint-plugin-security-rules",
  "version": "0.0.1",
  "description": "",
  "main": "./lib/index.js",
  "files": [
    "lib/**"
  ],
  "keywords": [
    "eslint",
    "eslintplugin"
  ],
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "watch": "yarn build --watch",
    "prepublish": "yarn build"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/eslint": "^7.28.0",
    "@types/estree": "^0.0.50",
    "@types/node": "^16.10.1",
    "typescript": "^4.4.3"
  },
  "peerDependencies": {
    "eslint": ">=0.8.0"
  }
}
